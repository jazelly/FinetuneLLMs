import commonTranslation from './common';
import workflowTranslation from './workflow';

const translation = {
  common: commonTranslation,
  workflow: workflowTranslation,
};

export default translation;
