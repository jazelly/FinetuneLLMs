import { FlipVertical } from '@phosphor-icons/react';
import React from 'react';

const AppIcon = ({ weight, size, color }) => {
  return <FlipVertical weight={weight} size={size} color={color} />;
};

export default AppIcon;
